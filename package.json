{
  "name": "jfrog-kiro-power",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "JFrog for Kiro — IDE Power (POWER.md + mcp.json + steering) and kiro-cli (skills) built from the vendored JFrog Agent Skills (Phase 1: knowledge only).",
  "license": "Apache-2.0",
  "scripts": {
    "sync-skills": "node scripts/sync-skills.mjs",
    "gen-steering": "node scripts/gen-steering.mjs",
    "sync-pin": "node scripts/sync-pin.mjs",
    "revendor": "npm run sync-skills && npm run gen-steering && npm run sync-pin",
    "install-scripts": "node scripts/install-scripts.mjs",
    "install-cli": "node scripts/install-cli.mjs",
    "verify-install": "bash scripts/verify-install.sh",
    "validate": "node scripts/validate.mjs",
    "test": "node --test scripts/validate.test.mjs"
  }
}
